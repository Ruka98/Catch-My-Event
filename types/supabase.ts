// Minimal Database type to avoid TS errors if you don't have generated types.
// Replace with your own generated types if available.
// npx supabase gen types typescript --project-id YOUR_PROJECT_ID > types/supabase.ts
export type Database = any;
